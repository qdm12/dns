package models

type (
	// Provider is a DNS over TLS server provider name
	Provider string
	// Host is the DNS host to use for TLS validation
	Host string
	// URL is an HTTP(s) URL address
	URL string
	// Filepath is a local filesytem file path
	Filepath string
)
